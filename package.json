{
  "name": "squad",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "squad": "tsx packages/cli/src/index.ts",
    "dev:server": "pnpm --filter @squad/server dev",
    "dev:web": "pnpm --filter @squad/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  }
}
